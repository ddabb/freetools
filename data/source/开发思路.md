---
title: FreeTools 开发思路
description: 随身工具宝小程序开发思路与技术选型分享
category: 产品设计
tags:
  - 开发
  - 技术选型
  - 产品
---

> 这是一个关于产品思考、技术选型、持续迭代的开发复盘�?
---

## 一、从一个「痛点」开�?
### 1. 我遇到的问题

作为一个产品经理，我每天都在和各种工具打交道：
- 算日期：Excel算、计算器算、手机算
- 转格式：JSON转XML、YAML转JSON
- 生成码：二维码、条形码
- 做图片：封面图、分享图

**每个需求都要找网站，每个网站都要看广告�?*

我开始思考：**能不能有一个地方，把这些小工具都集合在一起？**

### 2. 我的解决方案

```
问题：需要很多小工具，但没有好用的集�?解决方案：做一个工具集合小程序
```

但我不是为了做小程序而做小程序，我的核心诉求是：
1. **要免�?*：我自己也要用，不能收费
2. **要无广告**：受够了每个工具网站都弹广告
3. **要快**：打开就用，不要注册登�?4. **要好�?*：界面要好看，操作要流畅

---

## 二、技术选型：为什么选择「无后端」架构？

### 1. 传统�?B/S 架构问题

```
用户 �?前端界面 �?后端服务�?�?数据�?         ↑__________________�?              API 调用
```

**问题�?*
- 服务器成本：最便宜的云服务器也�?30 �?�?- 运维成本：需要监控、更新、安全防�?- 数据安全：用户数据存在服务器上，有泄露风�?
### 2. 我的「无后端」架�?
```
用户 �?小程序前�?�?本地计算（JavaScript�?                   �?              CDN（仅用于静态数据）
```

**优势�?*
- **零服务器成本**：所有计算都在用户手机上完成
- **零运维成�?*：不需要监控、不需要更新服务器
- **隐私安全**：用户数据只存在自己手机�?
### 3. CDN 在这个架构里的角�?
CDN（内容分发网络）在这个架构里扮演了「数据分发」的角色�?
```
我的数据文件（JSON�?    �?GitHub 仓库
    �?jsDelivr CDN（免费）
    �?小程序请�?```

**使用场景�?*
- 版本更新日志
- 预设题目（数独�?4点）
- 每日一言等静态数�?
**为什么不�?CDN 存储用户数据�?*
- 用户数据应该存在用户本地（手机）
- 不应该把用户数据存到服务�?
---

## 三、开发思路：如何组�?57+ 工具�?
### 1. 分包策略

微信小程序有「分包」机制，可以把代码分成多个包�?
```
主包（基础框架�?    �?生活工具包（12个工具）
    �?数学工具包（9个工具）
    �?文本工具包（9个工具）
    �?...
```

**好处�?*
- 用户首次打开只下载主包，速度�?- 用到哪个工具才下载对应的�?- 节省流量，加快加载速度

### 2. 工具分类设计

我按照「使用场景」分类，而不是「技术类型」分类：

| 分类 | 分类依据 | 举例 |
|------|---------|------|
| 生活工具 | 日常生活场景 | 日历、二维码、日期计�?|
| 数学工具 | 需要数学计�?| 数独�?4点、质数判�?|
| 文本工具 | 文本处理 | 字数统计、Markdown、密码生�?|
| 开发工�?| 程序员场�?| JSON转换、时间戳、颜色转�?|

**为什么这样分�?*

因为用户想的是「我今天要用什么工具」，而不是「我要学什么技术」�?
### 3. 工具卡片设计

每个工具用一个卡片展示：

```
┌─────────────────────────�?�?🧩 数独求解�?           �? �?图标 + 名称
�?快速解决数独难�?       �? �?简短描�?�?                       �?�?使用次数: 1,258        �? �?使用频率（可选）
└─────────────────────────�?```

**设计原则�?*
- 图标要直观：一看到就知道是什�?- 名称要简洁：5个字以内说清�?- 描述要准确：让人一眼知道用�?
---

## 四、核心功能的技术实�?
### 1. 数独求解�?
**算法：回溯法 + 约束传播**

```javascript
// 简化版思路
function solveSudoku(board) {
  // 1. 找到空格
  const empty = findEmpty(board);
  
  // 2. 如果没有空格，说明填完了
  if (!empty) return true;
  
  // 3. 尝试填入数字
  for (let num = 1; num <= 9; num++) {
    if (isValid(board, empty, num)) {
      board[empty] = num;
      
      // 4. 递归求解
      if (solveSudoku(board)) return true;
      
      // 5. 回溯
      board[empty] = 0;
    }
  }
  
  return false;
}
```

**优化：约束传�?*
- 先填「候选数最少」的格子
- 大部分数独可以在�?20-30 个格子后大幅加�?
### 2. 文案生图

**技术：Canvas 绑制**

```javascript
// 简化版思路
const ctx = canvas.getContext('2d');

// 1. 绘制背景
ctx.fillStyle = gradient;
ctx.fillRect(0, 0, width, height);

// 2. 绘制文字
ctx.font = 'bold 36px Noto Sans SC';
ctx.fillText(text, x, y);

// 3. 绘制二维�?ctx.drawImage(qrImage, qrX, qrY, qrSize, qrSize);

// 4. 导出图片
canvas.toTempFilePath();
```

### 3. 公众号配�?
**技术：Canvas + 响应式布局**

- 固定比例 2.35:1（公众号封面标准�?- 根据文字长度自动调整字号
- 8种配色方案可�?
---

## 五、数据更新机�?
### 1. 为什么需要更新机制？

有些数据需要更新：
- 版本日志（每次更新都要改�?- 每日一言（每天更新）
- 预设题目（可能扩充）

**但我不想搭建服务器…�?*

### 2. CDN + GitHub 方案

```
我修�?JSON 文件
    �?push �?GitHub
    �?jsDelivr 自动同步�?分钟�?    �?小程序请求获取最新数�?    �?缓存到用户手�?```

**实现代码�?*

```javascript
// 加载CDN数据
function loadData() {
  // 1. 检查缓�?  const cached = wx.getStorageSync('data_key');
  if (cached && !isExpired()) {
    return cached;
  }
  
  // 2. 请求CDN
  wx.request({
    url: 'https://cdn.jsdelivr.net/gh/xxx/data/xxx.json',
    success: (res) => {
      // 3. 缓存到本�?      wx.setStorageSync('data_key', res.data);
      // 4. 更新缓存时间�?      wx.setStorageSync('data_timestamp', Date.now());
    }
  });
}
```

### 3. 缓存策略

| 数据类型 | 缓存时间 | 更新频率 |
|---------|---------|---------|
| 版本日志 | 24小时 | 每次发版 |
| 每日一言 | 1小时 | 每天 |
| 预设题目 | 30�?| 偶尔更新 |
| 用户设置 | 永久 | 随时 |

---

## 六、迭代节�?
### 1. MVP 阶段（第1周）

先做核心功能�?- 10个最常用的工�?- 基础的分包加�?- 搜索功能

### 2. 功能完善（第2-4周）

根据用户反馈�?- 增加工具种类
- 优化界面交互
- 修复 Bug

### 3. 体验优化（第5�?�?
- 性能优化
- 加载速度优化
- 细节打磨

---

## 七、关键决策复�?
### �?正确的决�?
1. **选择无后端架�?*
   - 省去了服务器成本和运维成�?   - 数据隐私更安�?
2. **�?CDN 管理动态数�?*
   - 不需要服务器就能更新数据
   - 免费、全球加�?
3. **按场景分类工�?*
   - 用户更容易找到需要的工具
   - 符合用户的心智模�?
### ⚠️ 可以改进�?
1. **早期缺少用户反馈机制**
   - 应该早点加上「意见反馈」入�?   - 收集用户真实需�?
2. **文档不够完善**
   - 很多开发决策没有记�?   - 后来的人可能看不�?
---

## 八、写在最�?
这个小程序从想法到上线，大概花了 **1个月** 的业余时间�?
**我的经验是：**
- 先做 MVP，不要追求完�?- 用户反馈比代码更重要
- 小步快跑，持续迭�?
**如果你也有一个「做一个工具集合」的想法，不妨动手试试�?*

技术不是门槛，想法才是�?
---

> FreeTools 开发复盘，献给所有喜欢动手折腾的人�?