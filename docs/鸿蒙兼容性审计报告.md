# FreeTools 鸿蒙兼容性审计报告

> 审计日期：2026-04-26
> 项目：随身工具宝（freetools）微信小程序
> 审计范围：全部 161 个 JS 文件、全部 WXSS/WXML 文件、app.json、project.miniapp.json

---

## 一、总体评估

| 维度 | 状态 | 说明 |
|------|------|------|
| **project.miniapp.json** | ✅ 已配置 | mini-ohos SDK 0.5.1，mini-android SDK 1.6.24，mini-ios SDK 1.6.28 |
| **基础 API 层适配** | ✅ 已完成 | toast/modal/storage 均有 isHarmonyOS 分支（utils/toast.js, utils/modal.js, utils/storage.js） |
| **Canvas 绘制** | ⚠️ 部分完成 | 10/15 个 Canvas 页面已适配，5 个未适配 |
| **CSS 兼容性** | ⚠️ 存在问题 | -webkit- 前缀、backdrop-filter、scrollbar 自定义样式等 |
| **已废弃 API** | ❌ 需修复 | chooseImage、getBatteryInfo（异步版）、createCanvasContext（旧版） |
| **ECharts 组件** | ⚠️ 风险 | ec-canvas 使用旧版 Canvas + 新版 Canvas 双模式，鸿蒙兼容性不确定 |
| **音效系统** | ⚠️ 风险 | sound-manager.js 使用 getFileSystemManager + downloadFile 缓存音效 |

---

## 二、已完成适配（无需处理）

### 2.1 工具函数层 ✅

以下文件已实现 `isHarmonyOS` 检测和适配分支：

| 文件 | 适配内容 |
|------|---------|
| `utils/toast.js` | ohos.prompt.showToast 替代 wx.showToast |
| `utils/modal.js` | ohos.prompt.showDialog 替代 wx.showModal |
| `utils/storage.js` | ohos.data.preferences 替代 wx.setStorageSync/getStorageSync |
| `utils/imageGenerator.js` | Canvas 绘制适配 |

检测方式：
```js
const isHarmonyOS = typeof ohos !== 'undefined' ||
  (typeof window !== 'undefined' && typeof window.$element !== 'undefined');
```

### 2.2 Canvas 页面已完成适配 ✅

| 文件 | 适配方式 |
|------|---------|
| `packages/financial/pages/mortgage/mortgage.js` | isHarmonyOS 分支 + new Image() |
| `packages/financial/pages/retirementCalculator/retirementCalculator.js` | isHarmonyOS 分支 |
| `packages/health/pages/health-calculator/health-calculator.js` | isHarmonyOS 分支 |
| `packages/life/pages/battery-health/battery-health.js` | isHarmonyOS 分支 |
| `packages/life/pages/calendar/calendar.js` | isHarmonyOS 分支 |
| `packages/life/pages/copywriting/copywriting.js` | isHarmonyOS 分支 |
| `packages/life/pages/life-countdown/life-countdown.js` | isHarmonyOS 分支 + new Image() |
| `packages/life/pages/qrcode/qrcode.js` | 完整适配（createQRCodeHarmony/saveImageHarmony/drawCenterImageHarmony） |

---

## 三、需要修复的问题（按优先级排序）

### 🔴 P0 — 鸿蒙上直接崩溃或功能不可用

#### 3.1 Canvas 页面未做鸿蒙适配（6 个文件）

以下页面使用 Canvas 绘图但**没有** isHarmonyOS 分支：

| 文件 | Canvas 用途 | 影响 |
|------|-------------|------|
| `packages/food/pages/what-to-eat/what-to-eat.js` | 保存图片到相册 | ❌ 无法保存 |
| `packages/life/pages/article-cover/article-cover.js` | 公众号配图绘制+保存 | ❌ 无法保存 |
| `packages/life/pages/text-to-image/text-to-image.js` | 文字转图片 | ❌ 无法生成 |
| `packages/text/pages/emoji-to-png/emoji-to-png.js` | Emoji转PNG | ❌ 无法生成 |
| `packages/text/pages/text-to-png/text-to-png.js` | 文字转PNG | ❌ 无法生成 |
| `packages/text/pages/poster-generator/poster-generator.js` | 海报生成 | ❌ 无法生成 |

**修复方案**：参照 qrcode.js 的适配模式，为每个文件添加：
1. isHarmonyOS 检测
2. Canvas 绑定 `type="2d"`（鸿蒙仅支持新版 Canvas 2D）
3. 替换 `createCanvasContext()` 为 `createSelectorQuery().select().node()`
4. `canvas.createImage()` 替代 `wx.createImage()` / `new Image()`
5. saveImageToPhotosAlbum → 鸿蒙保存方案

---

#### 3.2 已废弃 API：chooseImage（2 个文件）

`wx.chooseImage()` 已废弃，应迁移到 `wx.chooseMedia()`。鸿蒙可能直接不支持。

| 文件 | 用途 |
|------|------|
| `packages/life/pages/qrcode/qrcode.js` | 选择Logo图片 |
| `packages/life/pages/text-to-image/text-to-image.js` | 选择背景图片 |

**修复方案**：
```js
// 旧
wx.chooseImage({ count: 1, success(res) { ... } })
// 新
wx.chooseMedia({ count: 1, mediaType: ['image'], success(res) { ... } })
```

---

#### 3.3 已废弃 API：getBatteryInfo 异步版（1 个文件）

`wx.getBatteryInfo()` 异步版已废弃，鸿蒙不支持。应使用 `wx.getBatteryInfoSync()`。

| 文件 | 用途 |
|------|------|
| `packages/life/pages/battery-health/battery-health.js` | 获取电池信息 |

---

### 🟡 P1 — 功能降级但不会崩溃

#### 3.4 旧版 Canvas API：createCanvasContext（4 个文件）

`wx.createCanvasContext()` 是旧版 Canvas API，鸿蒙 NEXT 仅支持新版 Canvas 2D（`type="2d"`）。

| 文件 | 用途 |
|------|------|
| `packages/financial/pages/retirementCalculator/retirementCalculator.js` | 图表绘制 |
| `packages/life/pages/calendar/calendar.js` | 日历截图 |
| `packages/life/pages/life-countdown/life-countdown.js` | A4纸截图 |
| `packages/math/components/ec-canvas/wx-canvas.js` | ECharts 旧版渲染 |

**注意**：ec-canvas 已有新版 Canvas 2D 的 fallback（wxml 中 `wx:if="{{isUseNewCanvas}}" type="2d"`），但 wx-canvas.js 中的旧版实现仍被保留。需确认鸿蒙上 SDKVersion 比较逻辑是否正确触发新版。

---

#### 3.5 ECharts 兼容性风险（1 个组件）

| 文件 | 风险点 |
|------|--------|
| `packages/math/components/ec-canvas/index.js` | 依赖 `wx.getSystemInfoSync().SDKVersion` 进行版本比较，决定使用新版/旧版 Canvas。鸿蒙的 SDKVersion 返回值不确定。 |

**建议**：在鸿蒙上强制使用新版 Canvas 2D，跳过版本比较。

---

#### 3.6 音效系统文件系统依赖（1 个文件）

`utils/sound-manager.js` 使用以下 API 缓存音效文件：

| API | 鸿蒙支持情况 |
|-----|-------------|
| `wx.getFileSystemManager().mkdirSync()` | ⚠️ 可能不支持递归创建 |
| `wx.getFileSystemManager().writeFile()` | ⚠️ 鸿蒙文件系统路径不同 |
| `wx.getFileSystemManager().rmdirSync()` | ⚠️ 递归删除 |
| `wx.downloadFile()` | ⚠️ 基本支持，但路径规则不同 |

**建议**：鸿蒙上直接使用 CDN URL 播放（不缓存），避免文件系统操作。

---

#### 3.7 加速度传感器（1 个文件）

| 文件 | API | 鸿蒙支持情况 |
|------|-----|-------------|
| `packages/life/pages/answer-book/answer-book.js` | startAccelerometer / stopAccelerometer / onAccelerometerChange | ⚠️ 需要添加鸿蒙适配 |

---

#### 3.8 openSetting 引导页（14 个文件）

`wx.openSetting()` 在鸿蒙上的行为可能不同。以下页面在保存图片失败时会引导用户手动授权：

涉及文件：mortgage、retirementCalculator、what-to-eat、health-calculator、article-cover、battery-health、calendar、copywriting、life-countdown、relative-calculator、text-to-image、emoji-to-png、poster-generator、text-to-png。

**建议**：鸿蒙上权限管理机制不同，需添加 isHarmonyOS 分支处理。

---

### 🟢 P2 — 样式/表现差异（不会崩溃，但可能显示异常）

#### 3.9 CSS `-webkit-` 前缀（30+ 个 WXSS 文件）

鸿蒙浏览器内核不是 WebKit/Blink，`-webkit-` 前缀可能无效。

**高频使用的 `-webkit-` 属性**：

| 属性 | 出现次数 | 鸿蒙兼容性 | 修复方案 |
|------|---------|-----------|---------|
| `-webkit-overflow-scrolling: touch` | 15+ 文件 | ❌ 不支持 | 鸿蒙默认弹性滚动，删除此属性 |
| `-webkit-box` / `-webkit-box-orient` / `-webkit-line-clamp` | 10+ 文件 | ⚠️ 可能不支持 | 替换为 `display: flex` + `overflow: hidden` + 标准属性 |
| `-webkit-scrollbar` 系列 | 15+ 文件 | ❌ 不支持 | 鸿蒙不支持自定义滚动条样式，添加 `@supports` 或直接删除 |
| `-webkit-background-clip: text` | 6 文件 | ⚠️ 可能支持 | 补充标准 `background-clip: text` |
| `-webkit-text-fill-color` | 5 文件 | ⚠️ 可能支持 | 补充标准 `color` 属性 |
| `-webkit-fit-content` | 1 文件 | ⚠️ 需验证 | 替换为 `fit-content` |

---

#### 3.10 backdrop-filter（21 个文件）

`backdrop-filter` 在部分鸿蒙版本上可能不支持（特别是 SDK 0.5.x）。

涉及文件：tool-card、json-xml-converter、yaml-json-converter、data-analyzer、color-converter、timestamp-converter、answer-book、constellation、life-countdown、qrcode、time-tool、zodiac、24point、gcd-lcm、sudoku-generator、sudoku-solver、mdconvert、password-generator、area-converter、base-converter、about、index。

**建议**：添加 fallback 背景：
```css
.glass-card {
  background: rgba(255, 255, 255, 0.85); /* fallback */
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}
```

---

#### 3.11 自定义滚动条样式（15+ 文件）

鸿蒙不支持 `-webkit-scrollbar` 系列属性，自定义滚动条样式将失效。**影响纯视觉**，不影响功能。可直接忽略或添加 `@supports` 降级。

---

#### 3.12 CSS var()（1 个文件）

仅 `packages/life/pages/quit-smoking/quit-smoking.wxss` 使用了 CSS 自定义属性。鸿蒙大概率支持，但需验证。

---

## 四、WXS 兼容性

### 4.1 filters.wxs

`utils/filters.wxs` 使用标准 WXS 语法（function/filter/sort），不依赖平台 API。**预期鸿蒙兼容**。

WXS 在鸿蒙上的已知限制：
- 不支持 ES6+ 语法（已遵守）
- 不支持异步操作（已遵守）
- `getDate()` 可能返回格式不同（本文件未使用日期）

---

## 五、componentFramework 配置

`app.json` 中 `"componentFramework": "glass-easel"` 已配置。

鸿蒙 NEXT 对 glass-easel 的支持情况需确认。如果鸿蒙不支持 glass-easel，需要：
1. 降级为 `"exparser"`（旧版）
2. 或等待鸿蒙适配 glass-easel

**建议**：在真机上测试 glass-easel 是否正常工作。

---

## 六、修复优先级建议

### 第一批（阻塞发布）
1. **6 个未适配 Canvas 页面**（3.1）— 添加 isHarmonyOS 分支
2. **chooseImage → chooseMedia**（3.2）— 2 个文件
3. **getBatteryInfo → getBatteryInfoSync**（3.3）

### 第二批（功能降级）
4. **createCanvasContext → Canvas 2D**（3.4）— 4 个文件
5. **ECharts 鸿蒙强制新版**（3.5）
6. **音效系统跳过文件缓存**（3.6）
7. **加速度传感器适配**（3.7）

### 第三批（样式优化）
8. **清理 -webkit-overflow-scrolling**（3.9）
9. **backdrop-filter 添加 fallback**（3.10）
10. **-webkit-line-clamp 标准化**（3.9）

### 不需要修复
- `-webkit-scrollbar` 系列 — 纯装饰，鸿蒙不影响功能
- box-shadow / transform / animation / gradient — 标准属性，鸿蒙支持良好
- flex 布局 — 鸿蒙完全支持

---

## 七、附录：文件统计

| 检查项 | 文件数 |
|--------|--------|
| 总 JS 文件 | 161 |
| 使用 Canvas 的页面 | 15 |
| 已做鸿蒙适配的 Canvas 页面 | 8 |
| 未做鸿蒙适配的 Canvas 页面 | 7 |
| 使用 -webkit- 前缀的 WXSS | 30+ |
| 使用 backdrop-filter 的 WXSS | 21 |
| 使用 vibrateShort/Long 的 JS | 31 |
| 使用 getFileSystemManager 的 JS | 8 |
| 使用 saveImageToPhotosAlbum 的 JS | 15 |
| 使用 openSetting 的 JS | 14 |

---

*报告由自动代码审计生成，基于 2026-04-26 代码快照。*
