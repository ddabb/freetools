/**packages/converter/pages/regex-tester/regex-tester.wxss**/
page {
  min-height: 100vh;
  background: linear-gradient(135deg, #ffeaa7 0%, #fab1a0 100%);
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Helvetica, Segoe UI, Arial, Roboto, 'PingFang SC', 'miui', 'Hiragino Sans GB', 'Microsoft Yahei', sans-serif;
}

.container {
  padding: 40rpx 30rpx;
  min-height: 100vh;
}

/* 通用区域样式 */
.regex-section,
.test-section,
.results-section,
.help-section {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10rpx);
  border-radius: 25rpx;
  padding: 40rpx 30rpx;
  margin-bottom: 40rpx;
  box-shadow: 0 15rpx 40rpx rgba(0, 0, 0, 0.15);
}

.section-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #333;
  margin-bottom: 30rpx;
  text-align: center;
}

/* 正则输入区域 */
.regex-input-container {
  margin-bottom: 30rpx;
}

.regex-input {
  width: 100%;
  background: #f8f9fa;
  border: 2rpx solid transparent;
  border-radius: 15rpx;
  padding: 28rpx 24rpx;
  font-size: 30rpx;
  color: #333;
  font-family: 'Monaco', 'Menlo', monospace;
  box-sizing: border-box;
  transition: all 0.3s ease;
  margin-bottom: 25rpx;
}

.regex-input:focus {
  border-color: #ffeaa7;
  background: white;
  box-shadow: 0 0 0 6rpx rgba(255, 234, 167, 0.1);
}

/* 标志位选择 */
.flags-container {
  display: flex;
  flex-wrap: wrap;
  gap: 20rpx;
}

.flag-item {
  display: flex;
  align-items: center;
  background: rgba(255, 234, 167, 0.1);
  padding: 15rpx 20rpx;
  border-radius: 10rpx;
  transition: all 0.3s ease;
}

.flag-item:active {
  background: rgba(255, 234, 167, 0.3);
}

.flag-text {
  font-size: 26rpx;
  color: #333;
  margin-left: 10rpx;
  font-weight: 500;
}

/* 常用正则区域 */
.quick-regex-section {
  border-top: 2rpx solid #f0f0f0;
  padding-top: 30rpx;
}

.quick-regex-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 20rpx;
}

.quick-regex-list {
  display: flex;
  flex-direction: column;
  gap: 15rpx;
}

.quick-regex-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: linear-gradient(135deg, #f8f9fa, #e9ecef);
  padding: 20rpx 25rpx;
  border-radius: 12rpx;
  transition: all 0.3s ease;
}

.quick-regex-item:active {
  background: linear-gradient(135deg, #ffeaa7, #fab1a0);
  transform: scale(0.98);
}

.quick-regex-item:active .regex-name,
.quick-regex-item:active .regex-preview {
  color: white;
}

.regex-name {
  font-size: 26rpx;
  font-weight: 600;
  color: #ffeaa7;
}

.regex-preview {
  font-size: 24rpx;
  color: #666;
  font-family: 'Monaco', 'Menlo', monospace;
}

/* 测试文本区域 */
.test-textarea {
  width: 100%;
  min-height: 200rpx;
  background: #f8f9fa;
  border: 2rpx solid transparent;
  border-radius: 15rpx;
  padding: 24rpx;
  font-size: 28rpx;
  color: #333;
  line-height: 1.5;
  box-sizing: border-box;
  transition: all 0.3s ease;
  margin-bottom: 25rpx;
}

.test-textarea:focus {
  border-color: #ffeaa7;
  background: white;
  box-shadow: 0 0 0 6rpx rgba(255, 234, 167, 0.1);
}

.test-controls {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 20rpx;
}

.test-btn {
  background: linear-gradient(135deg, #ffeaa7 0%, #fab1a0 100%);
  color: #d63031;
  border: none;
  border-radius: 15rpx;
  padding: 24rpx 40rpx;
  font-size: 28rpx;
  font-weight: 700;
  box-shadow: 0 10rpx 30rpx rgba(255, 234, 167, 0.4);
  transition: all 0.3s ease;
}

.test-btn:active {
  transform: translateY(-2rpx) scale(1.05);
  box-shadow: 0 15rpx 40rpx rgba(255, 234, 167, 0.5);
}

.test-stats {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.stat-item {
  font-size: 24rpx;
  color: #666;
  font-weight: 500;
}

/* 结果区域 */
.results-section {
  animation: fadeInScale 0.6s ease-out;
}

.result-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #333;
  margin-bottom: 25rpx;
  text-align: center;
}

.matches-list {
  max-height: 500rpx;
}

.match-item {
  background: rgba(255, 234, 167, 0.1);
  border-radius: 15rpx;
  padding: 25rpx;
  margin-bottom: 20rpx;
  border-left: 4rpx solid #ffeaa7;
}

.match-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 15rpx;
}

.match-index {
  font-size: 26rpx;
  font-weight: 700;
  color: #d63031;
}

.match-position {
  font-size: 24rpx;
  color: #666;
  font-family: 'Monaco', 'Menlo', monospace;
}

.match-content {
  margin-bottom: 15rpx;
}

.match-text {
  font-size: 28rpx;
  color: #333;
  font-family: 'Monaco', 'Menlo', monospace;
  background: rgba(255, 255, 255, 0.7);
  padding: 15rpx 20rpx;
  border-radius: 8rpx;
  word-break: break-all;
}

.match-groups {
  border-top: 1rpx solid rgba(255, 234, 167, 0.3);
  padding-top: 15rpx;
}

.groups-title {
  font-size: 24rpx;
  font-weight: 600;
  color: #d63031;
  margin-bottom: 10rpx;
  display: block;
}

.group-item {
  margin-bottom: 8rpx;
}

.group-text {
  font-size: 24rpx;
  color: #666;
  font-family: 'Monaco', 'Menlo', monospace;
}

/* 高亮显示 */
.highlight-text {
  background: #f8f9fa;
  border-radius: 15rpx;
  padding: 30rpx;
  line-height: 2;
  font-family: 'Monaco', 'Menlo', monospace;
  font-size: 28rpx;
  border: 2rpx solid #f0f0f0;
}

.text-part {
  padding: 4rpx 8rpx;
  border-radius: 6rpx;
}

.highlight-normal {
  background: transparent;
  color: #333;
}

.highlight-match {
  background: linear-gradient(135deg, #ffeaa7, #fab1a0);
  color: #d63031;
  font-weight: 600;
}

/* 无匹配结果 */
.no-matches {
  text-align: center;
  padding: 60rpx 30rpx;
}

.no-match-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
  display: block;
}

.no-match-text {
  font-size: 28rpx;
  color: #666;
  font-weight: 600;
}

/* 错误提示区域 */
.error-section {
  margin-bottom: 40rpx;
}

.error-box {
  background: linear-gradient(135deg, #ff9a9e, #fecfef);
  border: 2rpx solid #ff4757;
  border-radius: 20rpx;
  padding: 30rpx;
  display: flex;
  align-items: center;
  gap: 20rpx;
  position: relative;
}

.error-icon {
  font-size: 40rpx;
}

.error-text {
  flex: 1;
  font-size: 28rpx;
  color: #721c24;
  font-weight: 600;
}

.close-error-btn {
  width: 40rpx;
  height: 40rpx;
  border-radius: 50%;
  background: #ff4757;
  color: white;
  border: none;
  font-size: 24rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 帮助区域 */
.help-section {
  animation: fadeInUp 0.6s ease-out;
}

.help-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #333;
  margin-bottom: 25rpx;
  text-align: center;
}

.help-content {
  display: flex;
  flex-direction: column;
  gap: 15rpx;
}

.help-text {
  font-size: 26rpx;
  color: #555;
  line-height: 1.5;
  padding-left: 20rpx;
  position: relative;
}

.help-text:before {
  content: "•";
  position: absolute;
  left: 0;
  color: #ffeaa7;
  font-weight: bold;
}

/* 动画效果 */
@keyframes fadeInScale {
  from {
    opacity: 0;
    transform: scale(0.9);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}