<view class="container">
  <!-- 难度 -->
  <view class="diff-bar">
    <view class="pill {{difficulty==='easy'?'on':''}}" bindtap="setDifficulty" data-diff="easy">简单</view>
    <view class="pill {{difficulty==='medium'?'on':''}}" bindtap="setDifficulty" data-diff="medium">中等</view>
    <view class="pill {{difficulty==='hard'?'on':''}}" bindtap="setDifficulty" data-diff="hard">困难</view>
  </view>

  <!-- 顶栏 -->
  <view class="top-info">
    <view class="mode-toggle">
      <view class="mode-btn {{mode==='fill'?'on':''}}" bindtap="setMode" data-mode="fill">● 填充</view>
      <view class="mode-btn {{mode==='mark'?'on':''}}" bindtap="setMode" data-mode="mark">✕ 标记</view>
    </view>
    <view class="info-right">
      <text class="timer">⏱ {{timerText}}</text>
      <text class="progress">{{filledCount}}/{{totalFill}}</text>
    </view>
  </view>

  <!-- 加载 -->
  <view class="loading" wx:if="{{loading}}"><text>加载中...</text></view>

  <!-- 棋盘 -->
  <view class="board-wrap" wx:else>
    <view class="board" style="width:{{boardPx}}px;">
      <!-- 列提示 -->
      <view class="col-hints-row">
        <view class="corner" style="width:{{hintPx}}px;height:{{colHintH}}px;"></view>
        <view class="col-hint" wx:for="{{colHints}}" wx:for-index="cIdx" wx:key="*this" style="width:{{cellPx}}px;height:{{colHintH}}px;">
          <text class="hn" wx:for="{{item}}" wx:for-item="n" wx:key="*this">{{n}}</text>
        </view>
      </view>
      <!-- 行 -->
      <view class="row-group" wx:for="{{rowGroups}}" wx:for-item="rowItem" wx:key="uid">
        <view class="row-hint" style="width:{{hintPx}}px;height:{{cellPx}}px;">
          <text class="hn" wx:for="{{rowHints[rowItem.ri]}}" wx:for-item="n" wx:key="*this">{{n}}</text>
        </view>
        <view
          wx:for="{{rowItem.cells}}"
          wx:for-item="cell"
          wx:key="uid"
          class="{{cell.cls}}"
          style="width:{{cellPx}}px;height:{{cellPx}}px;"
          catchtouchstart="onTouchStart"
          catchtouchmove="onTouchMove"
          catchtouchend="onTouchEnd"
          data-row="{{rowItem.ri}}"
          data-col="{{cell.ci}}"
        >
          <text wx:if="{{cell.s === 2}}" class="mark-x">✕</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 操作 -->
  <view class="action-bar" wx:if="{{!loading}}">
    <view class="btn" bindtap="undo">↩ 重来</view>
    <view class="btn answer-btn" bindtap="onShowAnswer" wx:if="{{!showAnswer}}">💡 答案</view>
    <view class="btn answer-btn hide" bindtap="onHideAnswer" wx:if="{{showAnswer}}">🙈 隐藏</view>
    <view class="btn next" bindtap="nextLevel">▶ 下一题</view>
    <view class="btn sound-btn {{soundEnabled ? 'active' : ''}}" bindtap="toggleSound">
      {{soundEnabled ? '🔊' : '🔇'}}
    </view>
  </view>

  <!-- 题号与跳关 -->
  <view class="nav-row">
    <text class="puzzle-info">第 {{currentPuzzleIndex}}/{{maxPuzzles}} 题</text>
    <input class="jump-input" type="number" placeholder="跳转" value="{{jumpInputValue}}" bindinput="onJumpInput" bindconfirm="onJump" />
    <view class="btn-jump" bindtap="onJump">确定</view>
  </view>

  <!-- 通关弹窗 -->
  <view class="mask" wx:if="{{showWin}}" bindtap="closeWin">
    <view class="card" catchtap="">
      <view class="card-icon">🎉</view>
      <view class="card-title">通关！</view>
      <view class="card-time">用时 {{timerText}}</view>
      <view class="card-btn" bindtap="nextLevel">下一关 ▶</view>
    </view>
  </view>
  <!-- 搴曢儴骞垮憡 -->
  <ad unit-id="adunit-07e9c9da9e4b8254" ad-intervals="30" binderror="onAdError" bindclose="onAdClose"></ad>
</view>
